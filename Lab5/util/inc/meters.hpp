#ifndef METERS_13_HPP
#define METERS_13_HPP
#include <cstddef> //for size_t

/// @brief Increments counter of number of comparisons 
/// @param reset if true counter resets
/// @return current counter value
size_t numberOfComparisons(bool reset = false);

#endif